# LLM Configuration
# Agents will use Claude 3.5 Sonnet by default if ANTHROPIC_API_KEY is set
# Falls back to OpenAI if only OPENAI_API_KEY is provided
# System works without API keys but with reduced agent capabilities

# Anthropic API Key (for Claude models - preferred)
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# OpenAI API Key (for GPT models - fallback)
OPENAI_API_KEY=your-openai-api-key-here

# Model Selection (optional - defaults shown)
# LLM_PROVIDER=anthropic
# LLM_MODEL=claude-3-5-sonnet-20241022